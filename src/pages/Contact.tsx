import { useEffect } from 'react';
import { useNavigate } from 'react-router-dom';
import { Button } from '@/components/ui/button';
import { PortalLayout } from '@/components/PortalLayout';
import { useAuth } from '@/context/AuthContext';

export default function Contact() { const { user, isAuthenticated } = useAuth(); const navigate = useNavigate(); useEffect(() => { if (!isAuthenticated) navigate('/'); }, [isAuthenticated, navigate]); if (!user) return null; return <PortalLayout><h1 className="font-heading text-4xl text-navy md:text-5xl">Contact</h1><section className="mt-8 rounded-2xl bg-card p-6 shadow-sm"><p className="font-heading text-5xl text-navy">0800 XXX XXX</p><p className="mt-3 font-body text-muted-foreground">Mon-Fri 8am-5pm Waikato time</p><p className="mt-1 font-body text-deep-teal">support@midlandsleep.co.nz</p></section><section className="mt-6 rounded-2xl bg-card p-6 shadow-sm"><h2 className="font-heading text-2xl text-navy">Send a message</h2><div className="mt-4 rounded-2xl bg-deep-teal-pale p-4 font-body text-sm">Midland Sleep collects this information to respond to your support request under the Health Information Privacy Code 2020.</div><div className="mt-5 grid gap-4"><input className="rounded-full bg-muted/40 px-4 py-3 font-body" defaultValue={user.name} /><select className="rounded-full bg-muted/40 px-4 py-3 font-body"><option>Equipment support</option><option>Supply request</option><option>Delivery question</option></select><textarea className="min-h-32 rounded-2xl bg-muted/40 px-4 py-3 font-body" placeholder="Message" /><Button className="w-fit">Send Message →</Button></div></section></PortalLayout>; }
